{
  "name": "mlminiproject",
  "version": "1.0.0",
  "private": true,
  "description": "PowerEstimate - Electricity Bill Prediction System",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
